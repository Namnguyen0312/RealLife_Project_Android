<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="2dp"
        android:color="@color/black" /> <!-- Màu và độ rộng của đường viền -->
    <padding
        android:left="5dp"
        android:top="5dp"
        android:right="5dp"
        android:bottom="5dp" /> <!-- Khoảng cách từ đường viền đến nội dung -->

    <solid
        android:color="@color/white" /> <!-- Màu và độ rộng của đường viền -->

</shape>
